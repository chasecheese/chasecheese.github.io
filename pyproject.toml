[project]
name = "chasecheese-site"
version = "0.1.0"
description = "Static homepage generator + PDF resume renderer."
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.58,<2",
]

[tool.uv]
package = false
